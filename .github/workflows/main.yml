name: CI/CD Fake
on:
 push: 
 workflow_dispatch:
jobs:
    CI:
      runs-on: ubuntu-latest
      steps:
        - name: Build de Imagem Docker
          run: echo "Execução do comando de docker build"
        - name: Execução de Testes de Software
          run: ech "Aqui eu executo o teste de software"
        - name: Publicando a Release
          run: ech "Publicação da Relase final"
    CD:
      needs: [CI]
      uses: edymonte/repo-reaproveitavel-2/.github/workflows/cd.yml@main
      secrets: 
        ambiente: ${{secrets.AMBIENTE_ENTREGA}}
